import numpy as np
import math
from scipy.optimize import curve_fit 

#val_array is an array of arrays, each of which is a piece of the data to be fit
# want the user to pass in fit_funct (the function to be fit) and some set of data.
# the code should take in 
def computeFitForSNData(val_array, fit_funct):
    
    
